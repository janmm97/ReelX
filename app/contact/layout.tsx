import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Contact — Reelx',
  description: 'Get in touch with the Reelx support team.',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
